import 'package:flutter/material.dart';

class GetAStore extends StatelessWidget {
  const GetAStore({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('get a store'),
    );
  }
}
